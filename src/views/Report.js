
import { h } from 'hyperapp'

const Report = (state, actions) =>
  h('div', { class: 'report-container' }, [
    h('div', { class: 'report-user' }, [

    ]),
    h('div', { class: 'report-details' }, [
      h('div', { class: '' })
    ])
  ])

export default Report
